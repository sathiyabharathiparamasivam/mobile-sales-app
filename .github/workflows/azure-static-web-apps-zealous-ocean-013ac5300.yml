name: Disabled Old Static Web App Workflow

on:
  workflow_dispatch:

jobs:
  disabled_notice:
    runs-on: ubuntu-latest
    name: Disabled old workflow
    steps:
      - name: Explain workflow status
        run: echo "Use deploy-frontend-static-web-app.yml for the active frontend deployment."
