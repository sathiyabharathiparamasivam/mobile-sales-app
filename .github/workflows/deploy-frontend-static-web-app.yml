name: Disabled Duplicate Frontend Workflow

on:
  workflow_dispatch:

jobs:
  disabled_notice:
    runs-on: ubuntu-latest
    name: Disabled duplicate workflow
    steps:
      - name: Explain workflow status
        run: echo "Use azure-static-web-apps-zealous-ocean-013ac5300.yml for frontend deployment."
